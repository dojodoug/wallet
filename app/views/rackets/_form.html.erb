<%= form_for(@racket) do |f| %>
  <% if @racket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@racket.errors.count, "error") %> prohibited this racket from being saved:</h2>

      <ul>
      <% @racket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :entity %><br>
    <%= f.text_field :entity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
